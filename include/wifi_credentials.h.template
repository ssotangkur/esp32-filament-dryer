#pragma once

/**
 * @file wifi_credentials.h
 * @brief WiFi credentials configuration
 *
 * This is a template file. Copy this file to wifi_credentials.h
 * and replace the placeholder values with your actual WiFi credentials.
 *
 * IMPORTANT: Never commit wifi_credentials.h to version control.
 * It contains sensitive information and should remain local.
 */

// WiFi configuration
#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"

// OTA Update URL - Configure this to point to your firmware update server
#define OTA_URL "http://YOUR_LOCAL_SERVER_IP/firmware.bin"

// Firmware version - Update this when you release new firmware
#define FIRMWARE_VERSION "1.0.0"
