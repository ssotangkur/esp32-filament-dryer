# Development dependencies for VSCode/Pylance import resolution
# These match the packages installed in the Docker container
pytest>=7.0.0
pytest-embedded-serial>=1.0.0
pytest-embedded-idf>=1.0.0
pytest-embedded-qemu>=1.0.0

# Optional: Additional development tools
pytest-cov>=4.0.0
black>=22.0.0
flake8>=6.0.0
